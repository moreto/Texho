info:
  name: organizacaoLocal
  type: http
  seq: 1

http:
  method: GET
  url: "{{HOST}}/api/v1/organizacao/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
